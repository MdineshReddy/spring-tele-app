server:
  port: 9100
spring:
  application:
    name: CallDetailsMS
  cloud:
    discovery:
      hostname: localhost
    consul:
      config:
        enabled: true
        prefix: config
        defaultContext: application
        profileSeparator: '::'
        format: YAML
